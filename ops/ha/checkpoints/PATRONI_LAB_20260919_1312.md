# Patroni isolated existing-PGDATA takeover lab — 2026-09-19

Gate 1: PASS.

## Isolation
- Patroni scope: `mesflow-patroni-lab-20260919-1305`
- DCS namespace: `/mesflow-ha/`
- Docker network: `mesflow-patroni-lab-net`
- PostgreSQL published only on `127.0.0.1:65432`
- Patroni REST published only on `127.0.0.1:58008`
- Source and runtime use dedicated Docker volume `mesflow-patroni-lab-20260919-data`; no live TEST/HP PGDATA was mounted.

## Image/config
- Image: `mesflow-patroni:4.1.5-pg17`
- Patroni: 4.1.5
- PostgreSQL/pg_rewind/pg_basebackup in image: 17.11
- `patroni --validate-config`: PASS after using an internal Docker network alias for `connect_address`; host exposure remains loopback-only.

## Existing PGDATA source
- Restored from verified fresh backup `mesflow-auto-20260919-125715.dump`.
- Backup checksum: PASS.
- Source data: migration `0054_multi_open_session_per_employee`, 69 tables, operations=454, work_sessions=41.
- Source system identifier: `7687124184652935208`.
- Source timeline: 1.
- Replication role present.

## Patroni adoption evidence
- DCS lab scope was empty before Patroni start.
- Source container stopped with restart policy `no` before Patroni runtime started.
- Patroni adopted the same non-empty data volume; no initdb/cluster-creation lines appeared (`initdb_log_count=0`).
- System identifier after takeover: `7687124184652935208` (unchanged).
- First leader promotion advanced lab timeline 1 -> 2.
- Data remained migration `0054_multi_open_session_per_employee`, operations=454, work_sessions=41.
- REST: `/primary`=200, `/read-write`=200, `/replica`=503 while leader.
- `patronictl list`: one member `mesflow-lab-dell`, Leader, running.
- DCS keys created only below the lab scope: config/history/initialize/leader/member/status.
- PostgreSQL data/config/HBA are all under `/var/lib/postgresql/data`; `pg_hba_file_rules` reports 0 errors (7 rules).
- Patroni dynamic cache exists in PGDATA.

## Lifecycle test
- Stopping Patroni runtime made `/primary` unreachable; source remained stopped.
- Starting the same runtime returned `/primary` after about 6 seconds.
- System identifier stayed `7687124184652935208`; timeline advanced to 3 after re-promotion.
- Docker volume manager audit showed only `mesflow-patroni-lab-runtime` running against the volume; no second PostgreSQL manager.

## Live-system non-impact check
- Public MESFlow: ready, version 71.0.0.341, migration 0054.
- TEST: `pg_is_in_recovery=false`, `mesflow_hp_slot active=true`, streaming async, observed lag 0 bytes.
- HP: `pg_is_in_recovery=true`, WAL receiver streaming.

Conclusion: isolated Patroni takeover of an existing non-empty PGDATA is proven. Live Patroni takeover is now permitted only after production config diff and rollback are prepared; automatic failover remains OFF.
