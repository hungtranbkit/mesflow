const ProductionTrace=(()=>{
  let poId=null,before=null,allEvents=[];
  const cats=[['','Tất cả'],['PO,OPERATION','Sản xuất'],['SESSION','Session'],['QUANTITY','Sản lượng'],['DEFECT','Lỗi'],['REWORK','Rework'],['EXCEPTION','Ngoại lệ'],['CHANGE','Thay đổi']];
  const qLabel={GOOD:'Đạt',DEFECT:'Lỗi',REPAIRABLE:'Sửa được',REWORK_RECOVERED:'Đã phục hồi',SCRAP:'Phế'};
  const day=d=>new Intl.DateTimeFormat('vi-VN',{timeZone:'Asia/Ho_Chi_Minh',dateStyle:'full'}).format(new Date(d));
  const technical=m=>`<details><summary>Chi tiết kỹ thuật</summary><pre>${esc(JSON.stringify(m||{},null,2))}</pre></details>`;
  const auditDiff=e=>{const m=e.metadata||{},parse=x=>{try{return typeof x==='string'?JSON.parse(x):x||{}}catch(_){return {}}};const a=parse(m.before),b=parse(m.after);const ignored=new Set(['updated_at','created_at']);const keys=[...new Set([...Object.keys(a),...Object.keys(b)])].filter(k=>!ignored.has(k)&&JSON.stringify(a[k])!==JSON.stringify(b[k]));return keys.length?`<div class="trace-diff">${keys.map(k=>`<span><small>${esc(k.replaceAll('_',' '))}</small><b>${esc(a[k]??'—')} → ${esc(b[k]??'—')}</b></span>`).join('')}</div>`:''};
  const item=e=>`<article class="trace-event cat-${e.category.toLowerCase()}"><time>${new Intl.DateTimeFormat('vi-VN',{timeZone:'Asia/Ho_Chi_Minh',timeStyle:'medium'}).format(new Date(e.occurred_at))}</time><i></i><div><header><b>${esc(e.title)}</b><span>${esc(e.category)}</span>${e.source==='LEGACY_DERIVED'?'<em>Dữ liệu suy ra</em>':''}</header><p>${esc(e.description||'')}</p>${e.quantity_delta!=null?`<strong class="trace-qty ${e.quantity_delta<0?'negative':''}">${e.quantity_delta>0?'+':''}${e.quantity_delta}</strong>`:''}${e.category==='CHANGE'?auditDiff(e):''}<small>${esc(e.actor_name||'Hệ thống')}${e.operation_id?` · OP #${e.operation_id}`:''}${e.session_id?` · Session #${e.session_id}`:''}</small>${technical(e.metadata)}</div></article>`;
  const draw=()=>{const groups=new Map;for(const e of allEvents){const d=day(e.occurred_at);if(!groups.has(d))groups.set(d,[]);groups.get(d).push(e)}document.getElementById('ptTimeline').innerHTML=allEvents.length?[...groups].map(([d,rows])=>`<section class="trace-day"><h3>${esc(d)}</h3>${rows.map(item).join('')}</section>`).join(''):'<div class="empty">Chưa có sự kiện trace phù hợp.</div>'};
  async function load(reset=true){if(reset){before=null;allEvents=[]}if(!poId)return;const category=document.querySelector('.pt-filter.active')?.dataset.category||'';const params=new URLSearchParams({limit:'100'});if(category)params.set('category',category);if(before)params.set('before',before);const [d,q]=await Promise.all([api(`/api/production-orders/${poId}/trace?${params}`),api(`/api/production-orders/${poId}/quantity-history`)]);allEvents.push(...d.events);before=d.next_before;const c=d.context,r=q.reconciliation.current;document.getElementById('ptSummary').innerHTML=`<div><small>Production Order</small><h2>${esc(c.po_code)}</h2><span class="badge">${esc(c.status)}</span></div><div class="trace-current"><span><small>Kế hoạch</small><b>${c.planned_quantity||0}</b></span><span><small>Đạt</small><b>${r.good_qty||0}</b></span><span><small>Lỗi</small><b>${r.defect_qty||0}</b></span><span><small>Sửa được</small><b>${r.rework_qty||0}</b></span></div><div class="trace-coverage"><b>${q.reconciliation.matches?'Ledger khớp':'Ledger chưa bao phủ toàn bộ'}</b><small>Native trace từ MESFlow V68; dữ liệu cũ chỉ được suy ra một phần.</small></div>`;draw();document.getElementById('ptMore').hidden=!d.has_more}
  async function render(){title.textContent='Production Trace';subtitle.textContent='Dòng thời gian PO, Session, sản lượng, ngoại lệ và thay đổi';const pos=await api('/api/production-orders?limit=1000');content.innerHTML=`<div class="trace-toolbar panel"><label>Production Order<select id="ptPo"><option value="">Chọn PO cần truy vết</option>${(pos.items||[]).map(x=>`<option value="${x.id}">${esc(x.code)} · ${esc(x.product)}</option>`).join('')}</select></label><button class="btn" id="ptReload">Làm mới</button></div><section id="ptSummary" class="trace-summary"><div class="empty">Chọn một Production Order để xem lịch sử.</div></section><nav class="trace-filters">${cats.map(([v,l],i)=>`<button class="pt-filter ${i===0?'active':''}" data-category="${v}">${l}</button>`).join('')}</nav><main id="ptTimeline" class="production-trace"></main><button class="btn" id="ptMore" hidden>Tải thêm sự kiện</button>`;document.getElementById('ptPo').onchange=e=>{poId=Number(e.target.value)||null;load(true)};document.getElementById('ptReload').onclick=()=>load(true);document.getElementById('ptMore').onclick=()=>load(false);document.querySelectorAll('.pt-filter').forEach(b=>b.onclick=()=>{document.querySelectorAll('.pt-filter').forEach(x=>x.classList.remove('active'));b.classList.add('active');load(true)})}
  return {render,openPo:id=>{poId=Number(id);return render().then(()=>{document.getElementById('ptPo').value=String(poId);return load(true)})}};
})();
function renderProductionTrace(){return ProductionTrace.render()}
const openPageWithoutTrace=openPage;openPage=async function(id,btn){if(id==='production-trace'){setActive(btn||document.querySelector('[data-page="production-trace"]'));return renderProductionTrace()}return openPageWithoutTrace(id,btn)};
